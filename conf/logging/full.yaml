# @package _global_
logging:
  - _target_: pytorch_lightning.loggers.WandbLogger
    project: ${general.project_name}
    name: ${general.experiment_name}
    save_dir: ${general.save_dir}
    resume: "allow"
    id: ${general.experiment_name}_${now:%Y%m%d_%H%M%S}
