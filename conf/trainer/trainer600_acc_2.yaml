# @package _group_
deterministic: false
max_epochs: 1500
min_epochs: 1
check_val_every_n_epoch: 50
num_sanity_val_steps: 0
accumulate_grad_batches: 2
log_every_n_steps: 10
# overfit_batches: 0
devices: [0]
accelerator: gpu
